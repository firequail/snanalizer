<?xml version="1.0" encoding="utf-8"?>
<mx:Canvas xmlns:mx="http://www.adobe.com/2006/mxml" creationComplete="load()" width="100%" height="100%">
	<mx:Metadata>
	   [ Event( name="result", type="mx.rpc.events.ResultEvent") ]
	</mx:Metadata>

	<mx:RemoteObject id="recursosService" destination="recursosService"/>


	<mx:DataGrid id="recursos" dataProvider="{recursosService.getAll.lastResult}" doubleClickEnabled="true" width="100%" height="100%">
		<mx:columns>
			<mx:DataGridColumn headerText="Nombre" dataField="nombre"/>
			<mx:DataGridColumn headerText="Apellido" dataField="apellido"/>
			<mx:DataGridColumn headerText="Area" dataField="area"/>
		</mx:columns>
	</mx:DataGrid>
	
	<mx:Script>
		<![CDATA[
			import mx.controls.Alert;
			import mx.rpc.events.ResultEvent;
			
			public function load():void {
				recursosService.getAll();
			}
			
			/*private function verAtributos():void {
				dispatchEvent(ResultEvent.createEvent(datosMaestros.selectedItem));
				//Alert.show(datosMaestros.selectedItem);
			}*/
			
		]]>
	</mx:Script>
</mx:Canvas>
